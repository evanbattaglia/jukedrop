{
  "name": "jukedrop",
  "version": "0.0.2",
  "description": "",
  "dependencies": {
    "alt": "^0.18.5",
    "babel-core": "6.11.4",
    "babel-loader": "6.2.4",
    "babel-preset-es2015": "6.9.0",
    "babel-preset-react": "6.11.1",
    "react": "15.3.0",
    "react-dom": "15.3.0"
  },
  "devDependencies": {
    "webpack": "1.13.1",
    "webpack-merge": "0.14.1"
  },
  "scripts": {
    "start": "webpack -d --watch",
    "build": "webpack -d",
    "dist": "mkdir -p dist/static dist/public && webpack -p && cp -f index.html config.js.example dist/ && cp -f static/* dist/static/",
    "clean": "rm -rf dist"
  }
}
